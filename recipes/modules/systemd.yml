---
modules:
  - type: systemd
    system:
      enabled:
        - greetd.service
        - podman.socket
        - fcitx5.service
    user:
      enabled:
        - dms.service
        - dms-greeter-sync.service
        - dsearch.service
        - dms-wallpaper-set.service
        - dms-wallpaper-rotation.timer
        - topgrade.timer
        - podman.socket
